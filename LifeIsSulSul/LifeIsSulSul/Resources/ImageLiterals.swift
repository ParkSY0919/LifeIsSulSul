import SwiftUI

enum ImageLiterals {}

extension ImageLiterals {
    static let logo = Image(.logo)
    static let soju = Image(.soju)
    static let beer = Image(.beer)
    static let somek = Image(.somek)
}